get_filename_component(PLUGY_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${PLUGY_CMAKE_DIR})
list(REMOVE_AT CMAKE_MODULE_PATH -1)


if(NOT TARGET PLUGY::PLUGY)
    include("${PLUGY_CMAKE_DIR}/PLUGYTargets.cmake")
endif()

set(PLUGY_lIBRARIES PLUGY::PLUGY)